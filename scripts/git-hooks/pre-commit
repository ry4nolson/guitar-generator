#!/bin/sh
# Bump package.json to the latest numbered CHANGELOG heading and stage it.
cd "$(git rev-parse --show-toplevel)" || exit 1
node scripts/sync-package-version.mjs || exit 1
git add -- package.json
